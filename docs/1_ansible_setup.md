# 1. Установка Ansible

## Выполненные команды:
```bash
python3 --version  # Python 3.10.12
python3 -m pip install --user ansible
ansible --version   # Ansible 10.7.0
```

## Проверка:
- [x] Python 3.10+ установлен
- [x] Ansible установлен через pip
- [x] PATH настроен корректно
